# Model digests describe exact file bytes; do not normalize their line endings.
*.obj -text
*.mtl -text
*.stl -text
*.json -text

# Preserve upstream vendor sources and licenses byte for byte.
vendor/** -text -whitespace
